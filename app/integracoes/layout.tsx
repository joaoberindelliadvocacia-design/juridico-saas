import { AppShell } from '@/components/AppShell'

export default function IntegracoesLayout({ children }: { children: React.ReactNode }) {
  return <AppShell titulo="Integrações">{children}</AppShell>
}
