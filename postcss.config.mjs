/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: { config: "C:/juridico saas/juridico_saas/juridico-saas/tailwind.config.ts" },
    autoprefixer: {},
  },
};

export default config;
